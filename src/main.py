from vector_model import *

a = VectorModel()
print(
        a.run(query='what similarity laws must be obeyed when constructing aeroelastic models of heated high speed aircraft',
                dataset='cranfield')
)
